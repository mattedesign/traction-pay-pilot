
import { useUnifiedChatHandler } from "./useUnifiedChatHandler";

// Re-export the unified handler with the same interface for backward compatibility
export const useEnhancedChatHandlers = useUnifiedChatHandler;
